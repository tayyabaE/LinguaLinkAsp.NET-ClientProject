<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - LinguaLink</title>

    <style>
       body {
        margin: 0;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f4f7fa; /* Very light background for the whole page */
    }

    /* --- NAVIGATION BAR STYLING --- */
    nav {
        /* Primary blue color palette from before, now with a subtle gradient */
        background: linear-gradient(90deg, #1e3a8a, #3b82f6); /* Darker to lighter blue gradient */
        color: white;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px 60px; /* Slightly more vertical padding and wider horizontal padding */
        
        /* Stronger, more appealing shadow for depth */
        box-shadow: 0 6px 15px rgba(30, 58, 138, 0.4); 
        
        position: sticky;
        top: 0;
        z-index: 1000;
        border-bottom: 2px solid #2563eb; /* A subtle contrasting line */
    }

        nav .logo {
            font-size: 28px; /* Slightly larger logo */
            font-weight: 800; /* Extra bold */
            letter-spacing: 0.5px; /* Better typography for the brand name */
            /* Add a text shadow for a more premium look */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2); 
        }
        
        /* Navigation Links Container */
        nav .nav-links {
            display: flex; /* Ensure links are aligned */
            align-items: center;
        }

        nav a {
            color: #dbeafe; /* Very light blue for standard links */
            text-decoration: none;
            margin-left: 25px; /* Increased spacing between links */
            font-weight: 600; /* Semibold */
            font-size: 1.05rem; /* Slightly larger font */
            padding: 5px 0;
            position: relative;
            transition: color 0.3s, transform 0.2s;
        }

            /* Active/Hover Underline Effect */
            nav a::after {
                content: '';
                position: absolute;
                width: 0;
                height: 2px;
                bottom: -5px;
                left: 0;
                background-color: white;
                transition: width 0.3s ease-out;
            }

            nav a:hover {
                color: white; /* Pure white on hover */
                transform: translateY(-1px); /* Subtle lift effect */
            }
            
            nav a:hover::after {
                width: 100%; /* Line appears from left to right */
            }

    /* --- Container (Keep original structure, just use the new styling) --- */
    .container {
        max-width: 1200px;
        margin: 40px auto;
        padding: 0 20px;
    }

    /* Hero / Forms / Cards styling (adjusting to new body background) */
    h2 {
        text-align: center;
        margin-bottom: 20px;
        color: #1e3a8a; /* Ensure heading color is prominent */
    }

    .hero, .about-card, form {
        background-color: white;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0px 4px 15px rgba(0,0,0,0.1);
    }

        form {
            max-width: 400px;
            margin: 30px auto;
        }

            form input, form select, form button {
                width: 100%;
                padding: 12px;
                margin: 10px 0;
                border-radius: 6px;
                border: 1px solid #ccc;
                font-size: 16px;
            }

            form button {
                background-color: #3b82f6;
                color: white;
                border: none;
                cursor: pointer;
                transition: background 0.2s;
            }

                form button:hover {
                    background-color: #2563eb;
                }

        p.error {
            color: red;
            text-align: center;
        }
    </style>
</head>
<body>
    <nav>
        <div class="logo">LinguaLink</div>
        
        <div class="nav-links">
            <a href="@Url.Action("Index", "Home")">Home</a>
            <a href="@Url.Action("About", "Home")">About</a>
            <a href="@Url.Action("Courses", "Home")">Courses</a>
            <a href="@Url.Action("Register", "Account")">Register</a>
            <a href="@Url.Action("Login", "Account")">Login</a>
        </div>
    </nav>

    <div class="container">
        @RenderBody()
    </div>
</body>
</html>
