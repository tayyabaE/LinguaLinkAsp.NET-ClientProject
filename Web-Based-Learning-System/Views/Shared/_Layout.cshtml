<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <title>@ViewData["Title"] - LinguaLink</title>

    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f7fa;
        }

        .navbar-gradient {
            background: linear-gradient(90deg, #1e3a8a, #3b82f6);
        }

        .footer-gradient {
            background: linear-gradient(90deg, #3b82f6, #1e3a8a);
        }

        nav {
            background: linear-gradient(90deg, #1e3a8a, #3b82f6);
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 60px;
            box-shadow: 0 6px 15px rgba(30, 58, 138, 0.4);
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 2px solid #2563eb;
        }

            nav .logo {
                font-size: 28px;
                font-weight: 800;
                letter-spacing: 0.5px;
                text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
                display: flex;
                align-items: center;
            }

                nav .logo i {
                    margin-right: 8px;
                    color: white;
                }

            nav .nav-links {
                display: flex;
                align-items: center;
            }

            nav a {
                color: #dbeafe;
                text-decoration: none;
                margin-left: 25px;
                font-weight: 600;
                font-size: 1.05rem;
                padding: 5px 0;
                position: relative;
                transition: color 0.3s, transform 0.2s;
            }

                nav a::after {
                    content: '';
                    position: absolute;
                    width: 0;
                    height: 2px;
                    bottom: -5px;
                    left: 0;
                    background-color: white;
                    transition: width 0.3s ease-out;
                }

                nav a:hover {
                    color: white;
                    transform: translateY(-1px);
                }

                    nav a:hover::after {
                        width: 100%;
                    }

                nav a.active-link {
                    color: white;
                    font-weight: 700;
                    transform: translateY(-1px);
                }

                    nav a.active-link::after {
                        width: 100%;
                        background-color: white;
                    }
        .container {
            max-width: 1200px;
            margin: 40px auto;
            padding: 0 20px;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #1e3a8a;
        }

        .hero, .about-card, form {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 15px rgba(0,0,0,0.1);
        }

        form {
            max-width: 400px;
            margin: 30px auto;
        }

            form input, form select, form button {
                width: 100%;
                padding: 12px;
                margin: 10px 0;
                border-radius: 6px;
                border: 1px solid #ccc;
                font-size: 16px;
            }

            form button {
                background-color: #3b82f6;
                color: white;
                border: none;
                cursor: pointer;
                transition: background 0.2s;
            }

                form button:hover {
                    background-color: #2563eb;
                }

        p.error {
            color: red;
            text-align: center;
        }

        .site-footer {
            background: linear-gradient(90deg, #3b82f6, #1e3a8a);
            color: #e0e7ff;
            padding: 20px 20px 10px;
            border-top: 5px solid #2563eb;
            margin-top: 60px;
        }

        .footer-container {
            max-width: 1200px;
            margin: auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 30px;
        }

        .footer-brand h3 {
            color: white;
            font-size: 1.6rem;
            margin-bottom: 15px;
            letter-spacing: 1px;
            border-bottom: 2px solid #2563eb;
            display: inline-block;
            padding-bottom: 5px;
        }

        .footer-brand p {
            color: #bfdbfe;
            font-size: 0.9rem;
            max-width: 300px;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        .footer-links h4 {
            color: white;
            font-size: 1.15rem;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .footer-links a {
            display: block;
            color: #bfdbfe;
            text-decoration: none;
            margin-bottom: 10px;
            font-size: 0.95rem;
            padding-left: 0;
            transition: color 0.3s, padding-left 0.3s;
        }

            .footer-links a::before {
                content: ">>";
                margin-right: 8px;
                color: white; 
                font-weight: normal;
                opacity: 0.8;
                transition: margin-right 0.3s;
            }

            .footer-links a:hover {
                color: white;
                padding-left: 5px;
            }

        .social-icons {
            margin-top: 15px;
        }

            .social-icons a {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 40px;
                height: 40px;
                margin-right: 10px;
                border-radius: 50%;
                background-color: #2563eb;
                color: #fff;
                font-size: 1.1rem;
                border: 2px solid #3b82f6;
                transition: background 0.3s, transform 0.3s;
                text-decoration: none;
            }

                .social-icons a:hover {
                    transform: translateY(-2px);
                    background-color: #3b82f6;
                    box-shadow: 0 4px 10px rgba(59, 130, 246, 0.6);
                }

        .footer-bottom {
            text-align: center;
            margin-top: 20px; 
            padding-top: 15px;
            border-top: 1px solid #2563eb;
            font-size: 0.85rem;
            color: #93c5fd;
        }
    </style>
</head>
<body>
    <nav>
        <div class="logo"><i class="fas fa-link"></i>LinguaLink</div>

        <div class="nav-links">
            @{
                string currentController = ViewContext.RouteData.Values["Controller"].ToString();
                string currentAction = ViewContext.RouteData.Values["Action"].ToString();
            }

            <a href="@Url.Action("Index", "Home")" class="@((currentController == "Home" && currentAction == "Index") ? "active-link" : "")">Home</a>
            <a href="@Url.Action("About", "Home")" class="@((currentController == "Home" && currentAction == "About") ? "active-link" : "")">About</a>
            <a href="@Url.Action("Courses", "Home")" class="@((currentController == "Home" && currentAction == "Courses") ? "active-link" : "")">Courses</a>
            <a href="@Url.Action("Register", "Account")" class="@((currentController == "Account" && currentAction == "Register") ? "active-link" : "")">Register</a>
            <a href="@Url.Action("Login", "Account")" class="@((currentController == "Account" && currentAction == "Login") ? "active-link" : "")">Login</a>
        </div>
    </nav>

    <div class="container">
        @RenderBody()
    </div>

    <footer class="site-footer">
        <div class="footer-container">

            <div class="footer-brand">
                <h3>LinguaLink</h3>
                <p>
                    LinguaLink is a modern language learning platform designed
                    to help learners connect globally through interactive courses.
                </p>

                <div class="social-icons">
                    <a href="https://facebook.com" target="_blank" aria-label="Facebook">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="https://twitter.com" target="_blank" aria-label="Twitter">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a href="https://linkedin.com" target="_blank" aria-label="LinkedIn">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                    <a href="https://instagram.com" target="_blank" aria-label="Instagram">
                        <i class="fab fa-instagram"></i>
                    </a>
                </div>
            </div>

            <div class="footer-links">
                <h4>Pages</h4>
                <a href="@Url.Action("Index", "Home")">Home</a>
                <a href="@Url.Action("About", "Home")">About</a>
                <a href="@Url.Action("Courses", "Home")">Courses</a>
            </div>

            <div class="footer-links">
                <h4>Support</h4>
                <a href="@Url.Action("Contact", "Home")">Contact</a>
                <a href="@Url.Action("FAQ", "Home")">FAQ</a>
            </div>

            <div class="footer-links">
                <h4>Legal</h4>
                <a href="@Url.Action("Privacy", "Home")">Privacy Policy</a>
                <a href="@Url.Action("Terms", "Home")">Terms & Conditions</a>
            </div>

        </div>

        <div class="footer-bottom">
            © 2025 LinguaLink. All rights reserved.
        </div>
    </footer>

</body>
</html>